import React from 'react';
import { Text } from 'tamagui';

/**
 * A minimal example that only uses the Text component from Tamagui
 * with no theme dependencies
 */
export const MinimalTamagui = () => {
  return (
    <Text color="#011474" fontSize={16}>
      Minimal Tamagui Example
    </Text>
  );
};
